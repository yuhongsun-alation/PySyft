syntax = "proto3";

package syft.lib.torch;

import "proto/protobuffers/lib/torch/tensor.proto";

message ParameterProto {
  bytes tensor = 1;
  bool requires_grad = 2;
  // add optional back
  bytes grad = 3;
  // add optional back
  bytes grad_sample = 4; // opacus
}
